= content_for :title, "Timur - Browse - #{@record_name}"

#root
%script{ type: "text/javascript" }
  ! $(function() { React.render( React.createElement(Timur, { #{current_user ? "user: '#{current_user.name}', " : '' } mode: 'browser', model: '#{@model_name}', record: '#{@record_name}' }), $('#root')[0]); })
